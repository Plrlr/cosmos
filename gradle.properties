# Gradle JVM settings. Chaquopy unpacks and compiles Python for every ABI, so
# give the daemon a little headroom (default is 512m).
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

android.useAndroidX=true
android.nonTransitiveRClass=true
