<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- All feeds are fetched over https by Python's urllib. Reports and the
         downloaded update APK are written to app-private storage (filesDir),
         so no storage permission is needed. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Since Android 8.0 (API 26) an app can only hand an APK to the system
         package installer when it declares this permission. Without it the
         install intent is refused, which is what made the auto-update look
         like it "couldn't open the installer". The user still has to allow
         "install unknown apps" for Cosmos; the app asks for that separately. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Package visibility (Android 11+): let the app see the system package
         installer so the auto-update install intent can resolve. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
        <package android:name="com.android.packageinstaller" />
        <package android:name="com.google.android.packageinstaller" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cosmos">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.cosmos.app.UpdateFileProvider"
            android:authorities="com.cosmos.app.update"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>

</manifest>
